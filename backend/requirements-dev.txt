# Development / CI dependencies
# Install with: pip install -r requirements-dev.txt
# (assumes main requirements.txt is already installed)

# Testing
pytest~=8.3
pytest-asyncio~=0.25
pytest-cov~=6.1
anyio[trio]~=4.7
httpx~=0.27          # also used in production; listed here for explicitness

# Linting & formatting
ruff~=0.15
mypy~=1.14

# Type stubs
types-Pillow~=10.2
